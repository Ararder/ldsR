% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsid.R
\name{to_rsid}
\alias{to_rsid}
\title{Map CHR:POS to RSID}
\usage{
to_rsid(dset, build = c("guess", "37", "38"))
}
\arguments{
\item{dset}{a \code{\link[dplyr:reexports]{dplyr::tibble()}} with columns \code{CHR}, \code{POS}, \code{A1} and \code{A2}.
\code{CHR} can be given as \code{1} or \code{chr1}. Only autosomes are matched.}

\item{build}{genome build of \code{POS}: \code{"guess"} (default) picks the build with
the most matching variants, or pass \code{"37"} or \code{"38"}.}
}
\value{
\code{dset} with a \code{SNP} column, restricted to variants found in the reference
}
\description{
\code{to_rsid()} adds a \code{SNP} column with RSIDs to summary statistics that only
have genomic coordinates, so that they can be passed to \code{\link[=munge]{munge()}} and the
downstream \verb{ldsc_*()} functions.

Variants are matched on \code{CHR}, \code{POS} and the unordered allele pair
(\code{A1}/\code{A2} vs \code{REF}/\code{ALT}) against a bundled reference covering all SNPs in the
default LD score and weight files (HapMap3, dbSNP155 coordinates for GRCh37 and GRCh38).
Rows that do not match the reference are removed.
}
\examples{
\dontrun{
sumstats |>
  to_rsid() |>
  munge() |>
  ldsc_h2()
}
}
