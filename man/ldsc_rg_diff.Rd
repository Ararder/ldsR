% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ldsc_rg_diff.R
\name{ldsc_rg_diff}
\alias{ldsc_rg_diff}
\title{Test whether two genetic correlations differ}
\usage{
ldsc_rg_diff(pair1, pair2, weights = NULL, M = NULL, n_blocks = 200)
}
\arguments{
\item{pair1}{A list of two summary statistics tibbles defining the first
genetic correlation. Each tibble must contain \code{SNP}, \code{A1}, \code{A2}, \code{Z}, \code{N}.}

\item{pair2}{Same shape as \code{pair1}. May share a tibble with \code{pair1} (pass
the same data frame in both pairs to share a trait).}

\item{weights}{Optional, a data.frame or tbl with columns \code{SNP}, \code{L2}}

\item{M}{Optional, the number of SNPs in the reference panel}

\item{n_blocks}{Number of blocks to use for the jackknife estimator}
}
\value{
a \code{\link[dplyr:reexports]{dplyr::tibble()}} with the two global rGs, the global difference,
the jackknife-corrected difference, its standard error, and the z
statistic with chi-squared p-value.
}
\description{
Block jackknife test of the null hypothesis that the genetic correlation
between traits A and B equals the genetic correlation between traits C and D.
A trait may appear in both pairs (e.g. rG(A,B) vs rG(C,B)) by being passed
twice.

All summary statistics are merged with the weights to a single common SNP
set before either rG is computed, so the jackknife blocks correspond
one-to-one across the two correlations. This captures the covariance
between the two rGs, which is non-zero whenever a trait (or just SNPs)
is shared.

For each block \code{i}, delete values \verb{R(A,B)_i} and \verb{R(C,D)_i} for the rG
are differenced to form \code{D_i}. Pseudovalues are
\code{P_i = n * d - (n - 1) * D_i}, where \code{d = rG(A,B) - rG(C,D)} is the
global difference and \code{n} is the number of blocks. The test statistic is
\code{z = mean(P) / sqrt(var(P) / n)}.
}
\examples{
\dontrun{
# Four distinct traits
ldsc_rg_diff(
  pair1 = list(trait_a, trait_b),
  pair2 = list(trait_c, trait_d)
)

# Three traits: rG(A,B) vs rG(C,B), with B shared
ldsc_rg_diff(
  pair1 = list(trait_a, trait_b),
  pair2 = list(trait_c, trait_b)
)
}
}
